{
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../../packages/db && npx prisma generate && cd ../../apps/web && pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
